# D26060701-T07 验收收口与真实设备风险记录 Todo

更新时间：2026-06-07 00:58 +08:00

## 场景

- Scene：`uth-dev`
- Mode：`todo-breakdown`
- 来源 Design：`docs/work/D26060701-地理围栏完整能力设计/00-D26060701-design.md`
- 前置 Todo：`T01` 到 `T06`
- 执行方式：待用户决定；本轮不写 worker prompt，不实现代码。

## 目标

完成地理围栏完整能力的设计级收口：运行模块级和全局验证，记录真实设备/厂商 ROM/Play services/后台定位残余风险，写入每个已实施 Todo 的 Feedback，并为后续 `uth-review` 或 `uth-docs` handoff 准备证据。

## 已确认约束

- 本 Todo 不做新的功能扩张。
- 不把未完成的真实设备验证写成已覆盖。
- 不执行 Git 写入。
- 如果模块事实已经改变，标记 `Needs uth-docs scoped-sync`，但不在 `uth-dev` 中修改 `docs/context/`。

## 非目标

- 不新增功能。
- 不修复非地理围栏相关历史债务。
- 不归档任务包。
- 不提交、不推送、不建 PR。

## 允许修改范围

- `docs/work/D26060701-地理围栏完整能力设计/*feedback*.md`
- `docs/work/D26060701-地理围栏完整能力设计/runs/**`
- `docs/current-state.md`
- 必要时当前 Todo 范围内的测试小修复，但不得扩大功能范围。

## 必须完成

1. 汇总 Todo 完成状态
   - 确认 `T01` 到 `T06` 是否都有 Feedback。
   - 未完成项必须保留为 open，不写成已完成。

2. 运行验证命令
   - module geofence。
   - module settings。
   - module config。
   - architecture。
   - app focused geofence tests。
   - `clean assembleDebug`。

3. 扫描构建日志
   - warning。
   - deprecated。
   - exception。
   - failed。
   - 如果不是 0，需要解释是否是新增问题；没有用户 waiver 不能宣称完整通过。

4. 真实设备风险记录
   - 前台定位授权。
   - 后台定位授权。
   - 地图选择器。
   - 真实 enter/exit transition。
   - 厂商 ROM 后台限制。
   - 无 Play services 或 Play services 不可用场景。

5. current-state 更新
   - 只写索引级状态。
   - 标记设计级验收状态。
   - 标记 `Needs uth-docs scoped-sync`。

6. handoff
   - 若实现和验证完整，建议进入 `uth-review` 做 Design-level acceptance。
   - 若存在失败或真实设备阻塞，转入 `uth-debug` 或保留 open risk。

## 必须补记录

- 每个已实施 Todo 的 feedback：
  - changed files。
  - implementation summary。
  - verification commands。
  - pass/fail evidence。
  - risks。
  - rollback notes。

- 如验证命令输出复杂，写 run log：
  - `docs/work/D26060701-地理围栏完整能力设计/runs/R260607-*-verification.md`

## 验证命令

```powershell
.\gradlew.bat moduleGeofenceCheck --console=plain --no-daemon --stacktrace
.\gradlew.bat moduleSettingsCheck --console=plain --no-daemon --stacktrace
.\gradlew.bat moduleConfigCheck --console=plain --no-daemon --stacktrace
.\gradlew.bat architectureCheck --console=plain --no-daemon --stacktrace
.\gradlew.bat :app:testDebugUnitTest --tests "*Geofence*" --console=plain --no-daemon --stacktrace
.\gradlew.bat clean assembleDebug --console=plain --no-daemon --stacktrace
```

## 完成判定

- `T01` 到 `T06` 均有实现 Feedback，或未完成项明确保留 open。
- 自动化验证结果被记录，失败不被掩盖。
- 真实设备验证范围和残余风险被明确记录。
- current-state 指向正确下一场景：`uth-review`、`uth-debug` 或继续 `uth-dev`。
