# D26060701 T01 worker prompt - 模块与数据真源基线

派发时间：2026-06-07 11:49 +08:00

## 身份与工作方式

你是 `D26060701-T01` 的实现 worker。当前控制窗口只做总控和验收，不直接修改源码；本任务的代码实现由你负责。

必须遵守：

- Scene：`uth-dev`
- Mode：`todo-implementation`
- 不执行 Git 写入：不要 commit、push、checkout、reset、rebase、stash、创建/切换分支或 worktree。
- 你不是代码库里唯一的作者。工作区可能已有其他窗口或 worker 的改动；不要回滚、重排、清理与你任务无关的改动，遇到同文件既有改动时必须适配。
- 生产 DI 只能使用 Hilt，不得新增 service locator、static registry、global holder 或手写 runtime subgraph。
- 当前任务只实现 T01。不要实现 runtime 注册、定位权限、地图 UI、Agent prompt 创建、ConfigDetail 装载 UI 或插件 API。
- 如需要查询库/SDK/API 最新文档，按仓库规则优先使用 Context7；如果工具不可用，明确说明并优先用本地既有模式和官方文档。T01 预期主要复用本仓库已有 Room/Hilt/Gradle 模式，不应新增 Google Play services 或 Maps 依赖。

## 开始前必须读取

在写代码前至少读取：

- `AGENTS.md`
- `docs/README.md`
- `docs/current-state.md`
- `docs/context/01-验证构建治理.md`
- `docs/context/02-应用壳层与集成.md`
- `docs/context/03-核心基础与数据库.md`
- `docs/context/06-Provider配置Bot与Persona.md`
- `docs/work/D26060701-地理围栏完整能力设计/00-D26060701-design.md`

不要把 Design 或 Todo 中的“已设计”写成“已实现”；实现事实以本次源码改动和验证结果为准。

## 任务目标

建立地理围栏完整能力的模块骨架、domain contract、Room 真源和 repository 基线，使后续 runtime、UI、Agent 工具和 Config 装载都能复用同一套规则、区域、绑定和执行记录模型。

核心约束：

- “我的-地理围栏配置”是唯一真源；ConfigDetail 只保存装载引用，不复制坐标、半径或动作定义。
- 不把 geofence 塞入 Cron、Settings、Config 或 Plugin 既有 owner。
- `:feature:geofence:api` 不依赖 Android UI、Room、Google Play services、Maps SDK 或 Compose。

## 允许修改范围

只在以下范围内修改：

- `settings.gradle.kts`
- `build.gradle.kts`
- `app-integration/build.gradle.kts`
- `core/db/src/main/java/com/elymbot/android/data/db/**`
- `core/db/schemas/**`
- `app/schemas/**`
- `feature/geofence/api/**`
- `feature/geofence/data/**`
- `feature/geofence/impl/**`
- `app-integration/src/main/java/com/elymbot/android/app/integration/geofence/**`
- `app/src/test/java/com/elymbot/android/architecture/**`
- `app/src/test/java/com/elymbot/android/feature/geofence/**`
- `feature/geofence/data/src/test/java/**`

不要修改：

- `docs/context/**`
- 当前 task package 的 Design/Todo/Feedback/current-state
- runtime、presentation、Agent、ConfigDetail UI 或 plugin API 模块
- Manifest 定位权限或 receiver
- Google Play services / Maps SDK 依赖

如确实必须越过允许范围才能完成 T01，停止并以 `NEEDS_CONTEXT` 返回，说明原因和建议范围。

## 必须实现

1. 新增 Gradle 模块：
   - `:feature:geofence:api`
   - `:feature:geofence:data`
   - `:feature:geofence:impl`
   - 暂不新增 runtime/presentation 的依赖实现。

2. 新增 domain model：
   - `GeofenceRule`
   - `GeofenceRegion`
   - `ConfigGeofenceBinding`
   - `GeofenceExecutionRecord`
   - `GeofenceTransition`
   - `GeofenceActionType`
   - `GeofenceRuleStatus`

3. 新增 repository port：
   - `GeofenceRuleRepositoryPort`
   - 暴露 `rules`、`bindings`、create/update/delete、pause/resume、region 管理、config binding 管理、execution record 写入和查询。
   - 所有写入入口必须校验 rule/region 基本字段。

4. 新增 Room 真源：
   - `geofence_rules`
   - `geofence_regions`
   - `config_geofence_bindings`
   - `geofence_execution_records`
   - DB version 递增，补 migration 与 schema。

5. 新增 data store 与 mapper：
   - `FeatureGeofenceRuleRepositoryStore`
   - `FeatureGeofenceRuleRepositoryPortAdapter`
   - rule + regions 聚合读取时保持排序稳定。
   - config binding 只保存 `configId`、`ruleId`、`enabled`、`sortIndex` 等引用字段。

6. 新增 app-integration Hilt binding：
   - 绑定 repository port 到 data adapter。
   - 不让 `:app` 直接依赖 geofence data implementation。

7. 新增架构合同：
   - `:feature:geofence:api` 不依赖 Android、Room、Google Play services、Maps SDK。
   - plugin API / runtime 主线不新增 `hostApi.geofence.*`。
   - `core/**` 不依赖 geofence feature。

## 必须补测试

新增或扩展测试覆盖：

- `GeofenceRuleValidationTest`
  - 拒绝空名称。
  - 拒绝无 trigger 的 rule。
  - 拒绝非法纬度、经度。
  - 拒绝低于硬下限的半径。
  - 允许低于推荐值但高于硬下限的半径，并保留可由 UI 提示的不稳定状态。

- `FeatureGeofenceRuleRepositoryStoreTest`
  - create/update/delete rule。
  - 一条 rule 下多个 region 按 `sortIndex` 输出。
  - config binding 不复制 rule 内容。
  - execution record 可按 rule 查询最近记录。

- architecture/source contract
  - geofence API boundary。
  - plugin API 不暴露 geofence。
  - app-integration 才接 data implementation。

## 数据模型要求

Room 表字段按 Design T01 基线实现，至少覆盖以下字段。

`geofence_rules`：

- `ruleId: String`
- `name: String`
- `description: String`
- `enabled: Boolean`
- `triggerEnter: Boolean`
- `triggerExit: Boolean`
- `triggerDwell: Boolean`
- `dwellDelayMillis: Long`
- `actionType: String`
- `actionPrompt: String`
- `targetPlatform: String`
- `targetConversationId: String`
- `targetBotId: String`
- `targetConfigProfileId: String`
- `targetPersonaId: String`
- `targetProviderId: String`
- `minimumTriggerIntervalMillis: Long`
- `status: String`
- `lastRegisteredAt: Long`
- `lastTriggeredAt: Long`
- `lastError: String`
- `createdAt: Long`
- `updatedAt: Long`

`geofence_regions`：

- `regionId: String`
- `ruleId: String`
- `label: String`
- `latitude: Double`
- `longitude: Double`
- `radiusMeters: Float`
- `addressLabel: String`
- `sortIndex: Int`
- `createdAt: Long`
- `updatedAt: Long`

`config_geofence_bindings`：

- `configId: String`
- `ruleId: String`
- `enabled: Boolean`
- `sortIndex: Int`
- `createdAt: Long`
- `updatedAt: Long`

`geofence_execution_records`：

- `executionId: String`
- `ruleId: String`
- `regionId: String`
- `configId: String`
- `transition: String`
- `startedAt: Long`
- `completedAt: Long`
- `status: String`
- `errorCode: String`
- `errorMessage: String`
- `deliverySummary: String`
- `locationSnapshotJson: String`
- `triggerPayloadJson: String`

约束：

- 纬度范围 `[-90, 90]`，经度范围 `[-180, 180]`。
- 半径硬下限建议 `50m`；推荐最小值 `100m`。测试中应体现低于推荐值但高于硬下限可保存。
- active geofence 上限 100 是后续 runtime 任务处理；T01 可只在 model/validation 中提供常量或注释，不做注册逻辑。

## 验证命令

至少执行：

```powershell
.\gradlew.bat moduleGeofenceCheck --console=plain --no-daemon --stacktrace
.\gradlew.bat :feature:geofence:data:testDebugUnitTest --console=plain --no-daemon --stacktrace
.\gradlew.bat :app:testDebugUnitTest --tests "*Geofence*" --console=plain --no-daemon --stacktrace
.\gradlew.bat architectureCheck --console=plain --no-daemon --stacktrace
```

如果任一命令失败，先自行修复并重跑。不要只报告失败。

如果某个命令因本仓库当前结构不存在而失败，例如新增 module group 任务前不存在，先按本任务要求补齐构建入口再验证。

## 返回格式

完成后按以下格式返回：

```text
STATUS: DONE | DONE_WITH_CONCERNS | NEEDS_CONTEXT | BLOCKED

Changed files:
- ...

Implementation summary:
- ...

Verification:
- command: ...
  result: pass/fail
  warnings: N
  exceptions: N

Self-review:
- ...

Concerns or blockers:
- ...
```

只有在代码和要求的测试/构建验证都通过时才返回 `STATUS: DONE`。如果实现完成但仍有未覆盖的设备/人工风险，可返回 `DONE_WITH_CONCERNS` 并说明；不要把未运行的验证写成通过。
