# D26060501-T01 运行时兼容诊断模型与预检 Feedback

更新时间：2026-06-05 19:09 +08:00

## 状态

- Scene：`uth-dev`
- Mode：`todo-implementation`
- Todo：`T01 运行时兼容诊断模型与预检`
- 结果：已实现
- Todo 级验收：按用户要求跳过独立 Todo 级验收
- Git 写入：无

## 变更文件

- `core/runtime-container/src/main/java/com/elymbot/android/core/runtime/container/RuntimeCompatibilityProbe.kt`
- `core/runtime-container/src/test/java/com/elymbot/android/core/runtime/container/RuntimeCompatibilityProbeTest.kt`

## 实现摘要

- 新增 `RuntimeCompatibilityCategory`、`RuntimeCompatibilitySnapshot`、`RuntimeCompatibilityIssue`、`RuntimeCompatibilityProbe`。
- 新增 Android 环境采样 seam 与测试可注入 `RuntimeCompatibilityEnvironment`。
- 分类覆盖 rootfs 缺失、native runtime 缺失、app 私有目录执行受限、proot smoke 失败、外部存储 bind skipped、Android 13+ 通知权限拒绝。
- blocking 与 non-blocking issue 可并存，主 `category` 优先返回 blocking issue，否则返回第一个 warning。

## 验证

- 红灯：`.\gradlew.bat :core:runtime-container:testDebugUnitTest --tests "*RuntimeCompatibilityProbeTest*" --console=plain --no-daemon --stacktrace`
  - 结果：预期失败，新增 probe/model API 尚不存在。
- 绿灯：同一命令
  - 结果：pass。
- 回归：`.\gradlew.bat :core:runtime-container:testDebugUnitTest --console=plain --no-daemon --stacktrace`
  - 结果：`BUILD SUCCESSFUL in 45s`。
  - 日志：`build/uth-d26060501/01-core-runtime-container-testDebugUnitTest.log`
  - warning/deprecated/exception/failed 扫描：0 / 0 / 0 / 0。

## 未覆盖项

- 未做真实 API 29/30/31/33/34+ 设备验证。
- 未证明所有厂商 ROM 对 proot、seccomp、linker 或子进程限制的实际行为。

## 风险与回滚

- probe 会执行一次短超时 proot smoke；若真实设备上 smoke 过慢，可能增加启动前等待时间。
- 回滚方式：移除 `RuntimeCompatibilityProbe.kt` 与对应测试，并在 T03 回滚 service 接入。
