# D26052102 剩余插件 API 能力索引

更新时间：2026-05-21 17:05 +08:00

## 用途

本文件只作为 `D26052102` 的剩余能力索引，不是新增 Todo、不是 worker prompt，也不是实现计划。

它用于告知当前完成情况，并列出后续仍可派发的剩余代办。实际执行仍以 `T06` 到 `T12` 的详细 Todo 为准，派发顺序由用户决定。

## 当前完成情况

已完成并已有实现 / 验证记录：

| Scope | 状态 | 当前能力 |
| --- | --- | --- |
| `T01 宿主 API 异步桥与权限底座` | 已完成 | Host API facade、权限校验、审计、QuickJS await 基线 |
| `T02 宿主网络请求代理` | 已完成 | `hostApi.fetch` / `hostApi.network.request`，走宿主 `RuntimeNetworkTransport` |
| `T03 Provider 模型只读查询` | 已完成 | `hostApi.providers.list` / `hostApi.providers.models` |
| `T04 当前会话消息发送` | 已完成 | `hostApi.message.send`，当前会话约束 |
| `T05 当前会话历史只读查询` | 已完成 | `hostApi.conversation.history`，当前会话只读历史 |

已有基础能力，不作为本轮剩余缺口：

- message / command / regex handler 注册。
- lifecycle / LLM hook / tool / tool lifecycle 注册。
- plugin/session storage。
- settings / metadata / log。
- ToolSource 聚合与 host builtin tool 基础。

## 剩余代办

| Order | Todo | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | `T06 直接 LLM 调用` | 待派发 | 增加 `hostApi.callLlm` / `hostApi.llm.generate`，默认绕过插件 LLM hooks |
| 2 | `T07 上下文压缩落地` | 待派发 | 让 `compress_context` 不再返回 `context_compress_not_implemented`，并提供 `hostApi.context.compress` |
| 3 | `T08 插件定时任务回调` | 待派发 | 增加 `hostApi.registerScheduledHandler`，Cron wakeup 进入 Plugin V2 handler |
| 4 | `T09 插件流式输出` | 待派发 | 增加 `hostApi.message.openStream`，固定 App Chat / QQ fallback 语义 |
| 5 | `T10 富消息链` | 待派发 | 定义 `PluginMessageSegment`，补 text / image / file / mention / reply / card 映射 |
| 6 | `T11 Agent 能力注册` | 待派发 | 增加 `hostApi.registerAgent` 和受控 `agent.run` |
| 7 | `T12 Filter 组合表达式` | 待派发 | 增加 `allOf` / `anyOf` / `not` filter AST，保持旧 filters 兼容 |

## 不做项

继续保持用户已确认边界：

- 不做 Web API 注册。
- 不做平台适配器注册。
- 不做 DB 直连。
- 不做文本转图。
- 不做 HTML 渲染。
- 不暴露 AstrBot 风格别名。

## 派发提示

- 如果要逐项派发，直接选 `T06` 到 `T12` 中的目标 Todo。
- 如果要按依赖顺序连续推进，建议先做 `T06`，再做 `T07`。
- `T09` 与 `T10` 都会触碰消息发送 surface，不建议在同一个物理工作区并行。
- `T12` 相对独立，但若同时改 handler registry / evaluator，需要避免冲突。
