# D26052102 剩余插件 API 能力索引

更新时间：2026-05-22 00:35 +08:00

## 用途

本文件只作为 `D26052102` 的剩余能力索引，不是新增 Todo、不是 worker prompt，也不是实现计划。

它用于告知当前完成情况，并列出后续仍可派发的剩余代办。实际执行仍以详细 Todo 为准，派发顺序由用户决定。

## 当前完成情况

已完成并已有实现 / 验证记录：

| Scope | 状态 | 当前能力 |
| --- | --- | --- |
| `T01 宿主 API 异步桥与权限底座` | 已完成 | Host API facade、权限校验、审计、QuickJS await 基线 |
| `T02 宿主网络请求代理` | 已完成 | `hostApi.fetch` / `hostApi.network.request`，走宿主 `RuntimeNetworkTransport` |
| `T03 Provider 模型只读查询` | 已完成 | `hostApi.providers.list` / `hostApi.providers.models` |
| `T04 当前会话消息发送` | 已完成 | `hostApi.message.send`，当前会话约束 |
| `T05 当前会话历史只读查询` | 已完成 | `hostApi.conversation.history`，当前会话只读历史 |
| `T06 直接 LLM 调用` | 已验收 | `hostApi.callLlm` / `hostApi.llm.generate`，默认绕过插件 LLM hooks |
| `T07 上下文压缩落地` | 已验收 | `compress_context` 已接入宿主压缩实现，`hostApi.context.compress` 可用 |
| `T08 插件定时任务回调` | 已验收 | `hostApi.registerScheduledHandler`，Cron wakeup 进入 Plugin V2 handler |
| `T09 插件流式输出` | 已验收 | `hostApi.message.openStream`，App Chat editable 与 QQ final-on-close fallback |
| `T10 富消息链` | 已验收 | `PluginMessageSegment` 与 `hostApi.message.send({ chain })`，App Chat / QQ 映射与 fallback |

T06 到 T10 的验收命令：

- `.\gradlew.bat :feature:plugin:runtime:testDebugUnitTest --tests "*Llm*" --tests "*Context*Compress*" --tests "*Scheduled*" --tests "*Stream*" --tests "*MessageSegment*" --tests "*RichMessage*" --tests "*PluginV2HostApiQuickJsCapabilitiesTest*" --console=plain --no-daemon --stacktrace`：pass，日志 `build/reports/D26052102-T06-T10-feature-plugin-runtime-tests.log`。
- `.\gradlew.bat :app:testDebugUnitTest --tests "*FutureToolSourceRegistryContextCompressionInjectionTest*" --tests "*ContextStrategyToolSourceProviderTest*" --tests "*CronJobRunCoordinatorTest*" --tests "*PluginV2RuntimeLoaderTest*" --tests "*PluginV2HostApiArchitectureContractTest*" --tests "*PluginV2BootstrapHostApiTest*" --tests "*PluginV2HostApiPermissionPolicyTest*" --tests "*PluginV2HostApiAuditLoggerTest*" --console=plain --no-daemon --stacktrace`：pass，日志 `build/reports/D26052102-T06-T10-app-tests.log`。
- `.\gradlew.bat :app-integration:testDebugUnitTest --tests "*PluginHostCapabilityModuleTest*" --console=plain --no-daemon --stacktrace`：pass，日志 `build/reports/D26052102-T06-T10-app-integration-tests.log`。
- `.\gradlew.bat architectureCheck --console=plain --no-daemon --stacktrace`：pass，日志 `build/reports/D26052102-T06-T10-architectureCheck.log`。
- `.\gradlew.bat modulePluginCheck --console=plain --no-daemon --stacktrace`：pass，日志 `build/reports/D26052102-T06-T10-modulePluginCheck.log`。
- `.\gradlew.bat clean assembleDebug --console=plain --no-daemon --stacktrace`：pass，日志 `build/reports/D26052102-T06-T10-clean-assembleDebug.log`。

上述 6 个日志的 `warning` / `deprecated` / `exception` / `failed` 扫描计数均为 0。

已有基础能力，不作为本轮剩余缺口：

- message / command / regex handler 注册。
- lifecycle / LLM hook / tool / tool lifecycle 注册。
- plugin/session storage。
- settings / metadata / log。
- ToolSource 聚合与 host builtin tool 基础。

## 剩余代办

| Order | Todo | 状态 | 说明 |
| --- | --- | --- | --- |
| 1 | `T11 Agent 能力注册` | 待派发 | 增加 `hostApi.registerAgent` 和受控 `agent.run` |
| 2 | `T12 Filter 组合表达式` | 待派发 | 增加 `allOf` / `anyOf` / `not` filter AST，保持旧 filters 兼容 |

## 验收备注

- T06/T09 当前通过功能、权限、结构与构建验收；如后续需要更细审计口径，可补充 LLM provider/model/token usage 和 streamId/chunk/bytes/duration 的专用审计字段。该项不再作为 AstrBot 对齐能力缺口保留。
- T11 依赖 T06、T03 与现有 ToolSource / tool registry；T12 相对独立。

## 不做项

继续保持用户已确认边界：

- 不做 Web API 注册。
- 不做平台适配器注册。
- 不做 DB 直连。
- 不做文本转图。
- 不做 HTML 渲染。
- 不暴露 AstrBot 风格别名。

## 派发提示

- 如果要逐项派发，直接选 `T11` 或 `T12`。
- 如果要按依赖顺序连续推进，建议先做 `T11` 的 Agent 能力，再做 `T12` 的 filter AST；两者也可以分开派发。
- `T12` 若同时改 handler registry / evaluator，需要避免与后续 Agent handler surface 冲突。
