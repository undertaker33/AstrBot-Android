# D26052102 非阻塞审计字段收口 Feedback

## 基本信息

- Scene：`uth-dev`
- Mode：`formal-dev`
- Scope：T06/T09 非阻塞审计字段增强
- Worker：`019e4ebe-89f0-74b2-8a03-a6131939d3b5`
- Worker prompt：`docs/work/D26052102-插件API对齐AstrBot能力补齐/prompts/P260522-1612-nonblocking-audit-fields-worker.md`
- Git 写入：未执行
- 需求边界：不处理真实设备、真实网络、真实 provider、真实插件包验证；不暴露 AstrBot 风格别名；不实现 Web API 注册、平台适配器注册、DB 直连、文本转图、HTML 渲染。

## 实现结果

- `PluginV2HostApiFacade` 支持可选 `auditDetails` 与 `auditDetailsFromResult`，继续复用同一 permission / async / audit / structured error 通道。
- `PluginV2HostApiAuditLogger` 写入 `durationMs`，并对专用 detail 做敏感 key 过滤与必填审计字段覆写保护。
- T06 `hostApi.callLlm` / `hostApi.llm.generate` 审计补充 requested/selected provider/model、message/tool count、maxTokens、token usage 与 finishReason。
- T09 stream open / append / replace / close / fail 复用 facade 审计链路，审计补充 streamId、operation、chunk/bytes 计数、chunkBytes、next counters、platformMode、receiptId、markdown、streamDurationMs 与 failureCode。

## 主控复核

- Worker 原实现方向正确，但主控补强了两项：
  - detail map 不允许覆写 `api`、`permissionId`、`requestId`、`durationMs` 等必填审计字段。
  - stream audit 增加 `streamDurationMs`，记录从 open 到当前操作的生命周期时长。
- 未发现剩余非验证类 T06/T09 残留。
- 真实设备、真实网络、真实 provider、真实插件包验证仍按用户要求排除，不在本轮收口范围内。

## 验证

- `.\gradlew.bat :feature:plugin:runtime:clean :feature:plugin:runtime:testDebugUnitTest --tests "*PluginV2HostApiAuditLoggerTest.details_cannot_override_required_audit_fields" --console=plain --no-daemon --stacktrace`：pass。
- `.\gradlew.bat :feature:plugin:runtime:testDebugUnitTest --tests "*PluginV2MessageStreamApiTest.stream_audit_includes_stream_counters_bytes_platform_and_duration" --console=plain --no-daemon --stacktrace`：pass。
- `.\gradlew.bat :feature:plugin:runtime:testDebugUnitTest --tests "*PluginV2HostLlmApiTest*" --tests "*PluginV2MessageStreamApiTest*" --tests "*PluginV2HostApiAuditLoggerTest*" --console=plain --no-daemon --stacktrace`：pass。
- `.\gradlew.bat :app:testDebugUnitTest --tests "*PluginV2HostApiAuditLoggerTest*" --console=plain --no-daemon --stacktrace`：pass。
- `.\gradlew.bat architectureCheck --console=plain --no-daemon --stacktrace`：pass。
- `.\gradlew.bat modulePluginCheck --console=plain --no-daemon --stacktrace`：pass；命令输出包含既有 `PluginDataCleanupServiceTest` test stub 参数名 warning，不来自本轮修改文件。
- `.\gradlew.bat clean assembleDebug --console=plain --no-daemon --stacktrace`：pass；日志 `build/reports/D26052102-nonblocking-audit-fields-clean-assembleDebug.log`。
- `build/reports/D26052102-nonblocking-audit-fields-clean-assembleDebug.log` 的 `warning` / `deprecated` / `exception` / `failed` 扫描计数均为 0。
- `python tools/uth-hooks/uth-hook.py --event - --pretty` L3 closeout：pass；命中 `positive-claim-evidence-present` 与 `code-verification-clean`。

## 结论

T06/T09 验收时记录的非阻塞审计字段增强已收口。`D26052102` 当前设计内 `T01` 到 `T12` 无剩余非验证类 Todo 待派发；后续若要关闭整包，进入 Design-level 验收或按用户确认进入 `uth-git`。
