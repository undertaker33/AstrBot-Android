# Worker Prompt: D26052101-T01 应用内更新

你是 `uth-dev` worker。你不是唯一在代码库里工作的 agent，不要回滚、覆盖或清理他人无关改动。不得执行任何 Git 写入。

## 任务

读取并执行：

- Design：`docs/work/D26052101-应用内更新/00-D26052101-design.md`
- Todo：`docs/work/D26052101-应用内更新/10-D26052101-T01-todo-应用内更新.md`

目标是完成应用内更新功能，并让相关测试和编译通过。当前工作树已经有上一轮中断留下的半成品；你应在这些半成品基础上修正、补齐和验证，不要重新发明不必要的架构。

## 必须遵守

- 不执行 Git 写入：不 commit、不 branch、不 tag、不 push、不 merge。
- 不修改 `docs/context/`、ADR、changelog 或旧任务包。
- 不碰无关文件，尤其是当前已有但与本任务无关的 `feature/chat/runtime/src/main/res/values*/bot_command_strings.xml`。
- 不新增依赖。
- 生产依赖注入必须走 Hilt / constructor injection；不要新增 service locator、static registry、手写 app container。

## 允许修改范围

- `app/src/main/java/com/elymbot/android/update/**`
- `app/src/main/java/com/elymbot/android/MainActivity.kt`
- `app/src/main/AndroidManifest.xml`
- `app/src/main/res/xml/update_file_paths.xml`
- `app/src/main/res/values/strings.xml`
- `app/src/main/res/values-zh/strings.xml`
- `download/api/src/main/java/com/elymbot/android/download/DownloadModels.kt`
- `core/network/src/main/java/com/elymbot/android/core/runtime/network/RuntimeNetworkModels.kt`
- `app/src/test/java/com/elymbot/android/update/**`
- `app/src/test/java/com/elymbot/android/runtime/network/RuntimeNetworkModelsTest.kt`
- `app/src/test/java/com/elymbot/android/MainActivityPluginDeepLinkTest.kt`
- `app/src/test/java/com/elymbot/android/architecture/AndroidManifestRuntimeContractTest.kt`

## 重点修正

1. Release asset 选择必须严格：
   - debug build 只选包含 `debug` 的 APK。
   - release build 只选包含 `release` 且不包含 `debug` 的 APK。
   - release track 缺失时不能 fallback 到任意非 debug APK。

2. `暂不更新` 必须 tag-scoped：
   - 保存 `snoozedTagName + remindAfterEpochMillis`。
   - 仅当前 latest tag 命中时生效。
   - 新 tag 不能被旧 snooze 压住。

3. 失败弹窗不能只有关闭：
   - 至少提供 `重试`、`暂不更新`、`忽略此版本`。
   - 所有更新弹窗 `onDismissRequest = {}`。

4. 补测试：
   - policy/store/asset 选择。
   - runtime network capability/profile。
   - Manifest 的安装权限、FileProvider、cleanup receiver。
   - MainActivity 中启动一次检查、插件 deep link 优先级、update dialog host 接入。

5. 编译：
   - 修复所有 Kotlin/Compose/API 编译问题。

## 验证命令

至少运行：

```powershell
.\gradlew.bat :app:testDebugUnitTest --tests "com.elymbot.android.update.*" --console=plain --no-daemon --stacktrace
.\gradlew.bat :app:testDebugUnitTest --tests "*RuntimeNetworkModelsTest*" --tests "*MainActivityPluginDeepLinkTest*" --tests "*AndroidManifestRuntimeContractTest*" --console=plain --no-daemon --stacktrace
.\gradlew.bat :app:compileDebugKotlin --console=plain --no-daemon --stacktrace
```

如果时间允许，也可运行：

```powershell
.\gradlew.bat architectureCheck --console=plain --no-daemon --stacktrace
```

## 回传格式

```text
Status: DONE / DONE_WITH_CONCERNS / NEEDS_CONTEXT / BLOCKED

Changed files:
- ...

Implementation summary:
- ...

Verification:
- command: ...
  result: pass/fail/not run
  notes: ...

Concerns:
- ...
```
