# P260711-0337 T02 Worker Prompt：应用私有封面生命周期

## 任务定位

- Scene：`uth-dev`
- Mode：`todo-implementation`
- 当前 Todo：`T02 应用私有封面生命周期`
- 前置：T01 已实现并验证；以其当前代码与 Feedback 为接口事实。
- 工作目录：`C:\Users\93445\Desktop\Astrbot\ElymBot-Android-Native`
- 你是本 Todo 的唯一实现 worker；不设逐 Todo reviewer，必须严格自测、自检。

## 必读文件

按顺序完整读取：

1. `AGENTS.md`
2. `docs/README.md`
3. `docs/current-state.md`
4. `docs/work/D26071001-UI重构-Bot页/00-D26071001-design.md`
5. `docs/work/D26071001-UI重构-Bot页/20-D26071001-T02-todo-应用私有封面生命周期.md`
6. `docs/work/D26071001-UI重构-Bot页/11-D26071001-T01-feedback-Room多标签与封面元数据.md`
7. `docs/context/03-核心基础与数据库.md`
8. `docs/context/06-Provider配置Bot与Persona.md`
9. 直接相关代码、测试与模块说明。

Todo 是精确需求和写入边界。若 Design、Todo、T01 已落地接口或代码事实存在无法自行消解的冲突，立即回报 `NEEDS_CONTEXT` / `BLOCKED`，不得扩大 scope。

## 执行要求

1. 只实现 T02，不提前实现备份、目录 UI、详情页或裁切 UI。
2. 文件生命周期、安全边界、补偿顺序与状态行为使用 TDD：记录 RED 命令和预期失败，再完成 GREEN。
3. 受控目录、只读来源 URI、canonical containment、staging、commit/discard、失败补偿、删除与 orphan grace period 必须逐项有自动化证据。
4. 用户来源文件绝不能被删除或写入；不得新增 `ContentResolver.delete`、`DocumentFile.delete` 或来源写流。
5. 不新增图片依赖；生产 DI 只走 Hilt，不得静态 install/configure、service locator 或手写容器。
6. 不得执行任何 Git 写入，不得清理或覆盖 T01 及其他既有工作区改动。
7. 完成后运行 Todo 中全部验证命令；失败只在 T02 授权范围内收口。
8. 写 T02 Feedback：
   `docs/work/D26071001-UI重构-Bot页/21-D26071001-T02-feedback-应用私有封面生命周期.md`
9. governed Markdown 写入前后运行 `uth-utf8-guard`。Feedback 记录实现、文件、TDD、验证、warning/exception、风险和未验证项。
10. 仅在 T02 确实完成并有新鲜证据时把 `docs/current-state.md` 推进至 T03；不得宣称 Design 完成。

## 自检要求

回报前对照 T02 最终 `## Checklist`，并确认：

- API 合同保持 Android-free，data/integration 实现边界正确；
- 最长边 2048、方向纠正、可解码性与失败清理完整；
- Room 切换成功后才删旧私有文件，失败保留旧状态；
- orphan cleanup 只触达受控根且拒绝逃逸/符号链接；
- 无用户原文件删除路径、无新依赖、无静态生产入口；
- 验证日志没有忽略 warning、exception 或 failed marker。

## 回传格式

最终回复不超过 15 行：

- `Status: DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT`
- 修改文件概览
- RED/GREEN 摘要
- 验证命令结果摘要
- warning / exception / failed marker 计数
- Feedback 路径
- concerns / 未验证项
- 明确声明未执行 Git 写入

阻塞时必须直接说明阻塞点、已尝试内容和需要主窗口决定的问题。

## Rework 1 - 2026-07-11 16:23

Reason:

- 最终 reviewer 复查发现 `PersonaCoverAssetManager.commit()` 先 `renameTo(published)`，metadata 更新失败后删除 published，却让 `pending` 继续指向已不存在的 draft 路径。
- T05 UI 已保留 Failure draft 并提供重试，但底层真实 I/O 会因 draft 文件消失而使第二次 commit 必然失败。

Additional instructions:

1. 由原 T02 worker修复 commit 补偿，使 metadata 首次失败后仍保留一个完整、可读、可再次 commit 的 draft。
2. 可选择“复制到 published，metadata 成功后删除 draft”或“失败时可靠原子移回 draft”，但必须维持：成功后才删旧封面、失败保留旧 metadata/旧文件、失败临时 published 被清理。
3. pending map 与磁盘文件必须在失败、重试成功、discard、进程清理等路径一致，不得留下指向不存在文件的 entry。
4. 先补真实 manager I/O RED 测试：metadata store 第一次失败、第二次成功；验证首次失败旧封面不变、draft 仍存在可读，第二次成功，最终 draft 清理。另测失败后 discard 清理 draft。
5. 更新同一 T02 Feedback，追加 reviewer finding 与 Rework 1。current-state 标记为修复后需 T06 fresh 验证和同一 reviewer 复审。
6. 运行 T02 全部验证命令；不得 Git 写入，不得改 UI 作为规避。

Validation:

- manager 真实文件 I/O retry/discard 测试必须执行，不接受只测 presentation state。
- data/app-integration/app PersonaCover tests 与 architectureCheck 全通过，warning/exception/failed 为 0。

Return requirements:

- 明确描述 commit 文件顺序、首次失败后的磁盘/pending 状态和第二次成功证据。
