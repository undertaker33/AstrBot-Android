# P260711-1306 T03 Worker Prompt：备份恢复与老包兼容

## 任务定位

- Scene：`uth-dev`
- Mode：`todo-implementation`
- 当前 Todo：`T03 备份恢复与老包兼容`
- 前置：T01、T02 已实现并验证，以当前代码和 Feedback 为接口事实。
- 工作目录：`C:\Users\93445\Desktop\Astrbot\ElymBot-Android-Native`
- 你是本 Todo 唯一实现 worker；用户要求不设逐 Todo reviewer。

## 必读文件

完整读取：`AGENTS.md`、`docs/README.md`、`docs/current-state.md`、当前 Design、T03 Todo、T01/T02 Feedback、`docs/context/03-核心基础与数据库.md`、`docs/context/06-Provider配置Bot与Persona.md`、`docs/context/11-资源设置备份.md`，以及直接相关代码和测试。

精确路径：

- `docs/work/D26071001-UI重构-Bot页/00-D26071001-design.md`
- `docs/work/D26071001-UI重构-Bot页/30-D26071001-T03-todo-备份恢复与老包兼容.md`
- `docs/work/D26071001-UI重构-Bot页/11-D26071001-T01-feedback-Room多标签与封面元数据.md`
- `docs/work/D26071001-UI重构-Bot页/21-D26071001-T02-feedback-应用私有封面生命周期.md`

## 执行要求

1. 只实现 T03，不修改备份页面视觉，也不提前实现 T04/T05。
2. 备份格式、恢复冲突行为、损坏降级与补偿流程遵循 TDD，保留 RED/GREEN 命令和输出证据。
3. 完整备份与 Persona 模块备份都必须携带 tags、封面字节、crop、length、sha256；新包不得写设备绝对路径或旧 `tag` 真源。
4. 老 JSON-only、旧 ZIP、单 `tag`、缺少新增字段的包必须兼容；单图损坏只 warning + 默认封面，不中止整包。
5. 严格沿用三种现有冲突语义，不改变非 Persona 模块行为；Zip Slip、大小、hash、解码安全不得放宽。
6. 恢复文件先进入 T02 受控 staging；失败补偿不得提前删除旧封面。
7. 不得执行 Git 写入，不得清理 T01/T02 或其他既有改动，不得超出 Todo 允许范围。
8. 运行 T03 的全部验证命令，失败只在授权范围内收口。
9. 写 T03 Feedback：`docs/work/D26071001-UI重构-Bot页/31-D26071001-T03-feedback-备份恢复与老包兼容.md`。
10. governed Markdown 写入前后运行 `uth-utf8-guard`；仅在 T03 确实完成时把 `docs/current-state.md` 推进到 T04，不得宣称 Design 完成。

## 自检与回传

逐项核对 T03 最终 `## Checklist`，特别核对完整包/Persona 包、老包、单图损坏、三种冲突模式和 rollback 自动化证据。最终回复不超过 15 行，包含：

- `Status: DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT`
- 修改文件概览
- RED/GREEN 摘要
- 验证命令和 `warning / exception / failed marker` 计数
- Feedback 路径
- concerns / 未验证项
- 未执行 Git 写入声明

若 Design、Todo、前置接口或代码事实冲突，立即明确阻塞，不得猜测或扩大 scope。

## Rework 1 - 2026-07-11 13:26

Reason:

- 首轮已完成 manifest/ZIP、旧 `tag` 兼容和单图损坏降级，但 T03 的核心验收仍缺少三种冲突模式的封面归属、完整 rollback / 旧封面清理补偿矩阵及自动化证据。
- 因这些缺口，首轮 `DONE_WITH_CONCERNS` 不视为 Todo 完成，不能推进 T04。

Additional instructions:

1. 继续由同一 worker 从当前工作区完成剩余 T03，禁止回退或重做已经通过的部分。
2. 实现并测试：`MERGE_SKIP_DUPLICATES` 保留本地封面、覆盖重复采用 incoming 封面、`REPLACE_ALL` 清理恢复后未引用旧资产。
3. 补齐 Room 提交、staging 发布、后续失败的补偿 / rollback 与旧封面保留或清理证据。
4. 补齐 Todo 要求的旧 JSON-only、旧 ZIP、无 tags、无 cover、缺文件、hash 错误、不可解码图片和三种冲突模式矩阵；可复用既有测试 fixture，不得用弱断言代替行为证据。
5. 若现有 T02 port 无法支持必要补偿，先在 T03 允许的 `feature/persona/api` 备份 port/model 范围内做最小调整；若必须超出 Todo scope，立即 `NEEDS_CONTEXT`。
6. 完成后执行 T03 列出的全部验证命令，写 T03 Feedback，并把 current-state 推进到 T04；任一核心矩阵仍缺失时不得报 DONE。

Validation:

- 报告中逐项列出三种冲突模式和 rollback 测试名/结果。
- 运行 `:app:testDebugUnitTest --tests "*AppBackup*" --tests "*ModuleBackup*"`、`:app-integration:testDebugUnitTest --tests "*Backup*"`、`moduleSettingsCheck`、`architectureCheck`。
- 扫描 warning / exception / failed marker，并执行 governed Markdown post-write UTF-8 guard。

Return requirements:

- 按原回传格式；额外明确“冲突模式矩阵”和“rollback/旧封面清理矩阵”是否全部有自动化证据。
