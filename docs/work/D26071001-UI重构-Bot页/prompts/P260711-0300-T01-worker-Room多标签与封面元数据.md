# P260711-0300 T01 Worker Prompt：Room 多标签与封面元数据

## 任务定位

- Scene：`uth-dev`
- Mode：`todo-implementation`
- 任务包：`D26071001-UI重构-Bot页`
- 当前 Todo：`T01 Room 多标签与封面元数据`
- 工作目录：`C:\Users\93445\Desktop\Astrbot\ElymBot-Android-Native`
- 你是本 Todo 的唯一实现 worker；本轮不设逐 Todo reviewer，必须完成严格自测与自检。

## 必读文件

按顺序完整读取：

1. `AGENTS.md`
2. `docs/README.md`
3. `docs/current-state.md`
4. `docs/work/D26071001-UI重构-Bot页/00-D26071001-design.md`
5. `docs/work/D26071001-UI重构-Bot页/10-D26071001-T01-todo-Room多标签与封面元数据.md`
6. `docs/context/03-核心基础与数据库.md`
7. `docs/context/06-Provider配置Bot与Persona.md`
8. 直接相关的生产代码、测试与本地模块说明。

Todo 是本任务的精确需求与允许修改范围；Design 提供跨 Todo 约束。若二者或当前代码事实存在无法自行消解的冲突，立即以 `NEEDS_CONTEXT` 或 `BLOCKED` 回报，不得猜测扩展范围。

## 执行要求

1. 严格只实现 T01，不提前实现 T02–T06。
2. 行为、数据模型、Room migration 和 repository 改动遵循 `uth-sp-test-driven-development`：先写失败测试并记录 RED，再写最小实现并记录 GREEN。
3. 生产依赖注入只走 Hilt；不得引入静态 repository、service locator、手写容器或第二套生产 DI。
4. 不得执行任何 Git 写入，包括 commit、push、tag、merge、rebase、切换分支或 worktree 操作。
5. 不得覆盖或清理工作区中与 T01 无关的既有修改。
6. 若需要修改 Todo 允许范围之外的文件，停止并回报，不得自行扩大 scope。
7. 完成实现后执行 Todo 中全部验证命令；若命令失败，先在 T01 范围内收口。无法解决时如实回报，不得声称完成。
8. 按 UTH 正式任务规则写入 T01 Feedback，路径为：
   `docs/work/D26071001-UI重构-Bot页/11-D26071001-T01-feedback-Room多标签与封面元数据.md`
9. 修改 governed Markdown 前后必须运行 `uth-utf8-guard`。Feedback 需记录实现、变更文件、RED/GREEN、验证命令与结果、风险、未验证项，以及未执行 Git。
10. 更新 `docs/current-state.md`：仅在 T01 确实完成并有新鲜验证证据时，将当前 Todo 推进到 T02；不得宣称 Design 完成。

## 自检要求

回报前逐项对照 T01 的最终 `## Checklist`，并检查：

- `PersonaProfile.tags` 与统一规范化语义一致；
- `persona_tags` / `persona_cover_assets` aggregate、DAO、mapper、repository 和 cascade 行为完整；
- 23 → 24 migration 去除旧 `tag` 双真源；
- 两处 schema 24 一致；
- 直接构造与 legacy 兼容调用已在允许范围内收口；
- 验证输出中没有被忽略的 warning、exception 或 failed marker。

## 回传格式

最终回复控制在 15 行内，并包含：

- `Status: DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT`
- 实际修改文件概览
- RED/GREEN 一行摘要
- 验证命令与结果一行摘要
- warning / exception 计数
- Feedback 路径
- concerns / 未验证项
- 明确声明未执行 Git 写入

若为 `BLOCKED` 或 `NEEDS_CONTEXT`，必须在回复中直接说明阻塞点、已尝试内容和需要主窗口决定的问题。
