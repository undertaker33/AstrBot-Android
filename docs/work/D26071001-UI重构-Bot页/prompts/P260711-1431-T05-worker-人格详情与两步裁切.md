# P260711-1431 T05 Worker Prompt：人格详情与两步裁切

## 任务定位

- Scene：`uth-dev`
- Mode：`todo-implementation`
- 当前 Todo：`T05 人格详情与两步裁切`
- 前置：T01/T02/T04 已提供字段、cover draft/commit 与目录编辑事件。
- 工作目录：`C:\Users\93445\Desktop\Astrbot\ElymBot-Android-Native`
- 你是本 Todo 唯一实现 worker；不设逐 Todo reviewer。

## 必读与视觉基准

完整读取 `AGENTS.md`、`docs/README.md`、`docs/current-state.md`、Design、T05 Todo、T01/T02/T04 Feedback、`docs/context/02-应用壳层与集成.md`、`docs/context/06-Provider配置Bot与Persona.md`、直接相关代码与测试。

必须实际查看：

- `docs/work/D26071001-UI重构-Bot页/Image/定稿/角色详情页定稿.png`

图片文件名保留历史名称，但生产页面标题和所有用户文案必须为“编辑人格”/“人格”。不得照搬图中的不存在字段。

## 执行要求

1. 只实现 T05：二级详情页、五字段、两态面板、动态取景、Photo Picker/SAF 与纵向→方形两步裁切。
2. state machine、gesture policy、表单校验、crop flow 先 TDD；纯 Compose 视觉按 UI/source contract 验证。
3. 只显示名称、最多三标签、系统提示词、上下文消息数、启用状态；不得展示 default model、enabled tools 或其他概念字段。
4. 系统提示词固定高度，超长只内部滚动；标签逗号输入最多形成三项并统一走 normalization。
5. 面板只稳定停在 `EXPANDED` / `COLLAPSED`；拖动 transient offset 不持久化，释放按阈值/速度吸附；把手实际触控高度至少 48dp。
6. 展开/收起时封面分别以对应实际可见区域动态居中，动画与重取景同步。
7. 选图只调用 T02 draft；依次独立裁纵向、方形，方形不得从纵向结果二次裁切。取消/退出 discard，commit 失败保留旧封面。
8. 二级页无全局底栏、搜索、筛选或更多；目录模式一/详情消费 portrait，模式二消费 square，缺失使用中心默认值。
9. 不引入第三方裁切库，不改 Room/备份格式，不执行 Git 写入，不覆盖前置或其他改动，不得超 scope。
10. 运行 T05 全部验证；写 `docs/work/D26071001-UI重构-Bot页/51-D26071001-T05-feedback-人格详情与两步裁切.md`，governed Markdown pre/post UTF-8 guard；完成后推进 current-state 到 T06。

## 自检与回传

逐项核对 T05 最终 `## Checklist`，特别验证：返回/保存、五字段、固定 prompt、面板吸附、两种动态中心、两步独立 crop、取消/失败旧封面保留、二级页无底栏。

最终回复不超过 15 行，包含 `Status`、修改概览、RED/GREEN、验证及 warning/exception/failed 计数、Feedback、未验证设备/手势/视觉项、未执行 Git。若需新增依赖或超 Todo scope，立即 `NEEDS_CONTEXT`。

## Rework 1 - 2026-07-11 14:47

Reason:

- 首轮实现把裁切缩放做成滑杆，但 Design/T05 明确要求裁切区支持拖动与双指缩放。滑杆不能作为 pinch-to-zoom 的替代实现。
- `docs/current-state.md` 已被提前推进到 T06，须在本 rework 完成前保持 T05 未闭环语义；不得以首轮 Feedback 声称规格全部满足。

Additional instructions:

1. 继续由同一 worker 修改当前 T05 实现，把纵向和方形裁切区都接入真实多点触控 pinch-to-zoom；拖动与缩放必须作用于同一 draft 的各自独立 `PersonaCropSpec`。
2. 缩放范围与 crop spec 校验保持一致，手势中心变化不得产生 NaN/Infinity 或越界中心；必要时以纯函数抽出 gesture delta → crop spec，先补 RED/GREEN 测试。
3. 移除作为主缩放交互的滑杆；如保留任何辅助控件，不能替代或干扰双指缩放，且不得偏离定稿视觉。
4. 纵向步骤与方形步骤都要有自动化 state/gesture 证据；返回纵向步骤仍保留各自独立 crop 状态。
5. 更新同一 T05 Feedback，明确记录首轮偏差与 rework；重新运行 T05 全部验证和 UTF-8 guard。
6. rework 未完成前 current-state 不得保持“T05 完成”的错误结论；完成并验证后才可重新推进 T06。

Validation:

- 报告 pinch gesture policy 的 RED/GREEN 证据和测试名。
- 重新运行 T05 Todo 的 presentation/app/modulePersonaCheck/architectureCheck 全部命令。
- warning / exception / failed marker 均扫描为 0。

Return requirements:

- 明确说明滑杆是否移除、纵向/方形是否都支持真实 pinch、两套 crop 是否独立保持。

## Rework 2 - 2026-07-11 15:46

Reason:

- 最终 reviewer 发现详情 `EditorCover` 始终 `Alignment.Center`，没有消费已持久化的 portrait crop 中心与 zoom，详情页最终渲染未闭环。
- commit 失败把状态替换为只含 message 的 `Failure`，丢失 draft 与两套 crop；退出只 discard `Portrait`/`Square`，导致失败 draft 留在 pending/staging，且用户无法重试。

Additional instructions:

1. 由原 T05 worker 修复详情封面渲染，复用或对齐 T04 已落地的 crop render policy，同时应用 portrait center + zoom；展开/收起 viewport 动态取景仍须保持。
2. 重构失败态以保留 draft、portraitCrop、squareCrop 与错误信息；提供明确重试 commit 和取消/退出 discard 路径。
3. commit 失败必须保留旧封面；重试成功后正确发布；取消或页面退出必须立即 discard staging/pending，不只依赖 orphan cleanup。
4. 先补 RED 测试：非默认 portrait crop 进入详情渲染；commit 失败后 draft 可重试；失败态退出触发 discard；旧 cover 不变。
5. 更新同一 T05 Feedback，追加最终 reviewer finding 与 Rework 2 证据。
6. 运行 T05 全部验证命令；不得 Git 写入，不得改动 T04 已完成的方卡 finding。

Validation:

- 明确列出详情 zoom/center 渲染测试和失败→重试/退出 discard 测试。
- presentation/app/modulePersonaCheck/architectureCheck 全通过，warning/exception/failed 为 0。

Return requirements:

- 明确说明 reviewer 的 Critical 详情部分与 Important draft lifecycle finding 是否均已修复。
