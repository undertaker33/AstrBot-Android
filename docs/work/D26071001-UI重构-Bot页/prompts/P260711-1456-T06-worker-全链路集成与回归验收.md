# P260711-1456 T06 Worker Prompt：全链路集成与回归验收

## 任务定位

- Scene：`uth-dev`
- Mode：`todo-implementation`
- 当前 Todo：`T06 全链路集成与回归验收`
- 前置：T01–T05 已完成；T03/T05 分别经历 rework 并已收口。
- 工作目录：`C:\Users\93445\Desktop\Astrbot\ElymBot-Android-Native`
- 你是最后一个实现/集成 worker；用户要求全部完成后才派 reviewer。

## 必读范围

完整读取 `AGENTS.md`、`docs/README.md`、`docs/current-state.md`、Design、T06 Todo、T01–T05 全部 Feedback、`docs/context/01-验证构建治理.md`、`02-应用壳层与集成.md`、`03-核心基础与数据库.md`、`06-Provider配置Bot与Persona.md`、`11-资源设置备份.md`，以及直接相关代码与测试。

同时实际查看全部定稿图，用于验收追踪，但不要把无法自动验证的视觉项伪装为已验证。

## 执行要求

1. 建立 Design requirement → 当前测试/命令/设备项的验收追踪；缺少可自动化证据的条目先补测试。
2. 复跑 T01–T05 窄测试与 T06 全部验证命令，不依赖旧增量结果。
3. 验证 Room 23→24、双 schema、文件生命周期、用户原图保护、完整/Persona 备份、老包、冲突/rollback、双模式、顶栏、详情、两态面板、动态居中、两步 crop 与 pinch。
4. 验证机器人页、模型页、外层滑动、全局底栏、Persona 引用保护、runtime context、备份其他模块无回归；用户可见文案保持“人格”，无新增 Role 页面/路由。
5. 发现明确集成缺陷时只在 T01–T05 原授权范围内修复并补回归测试；未知失败不要猜测，回报 `BLOCKED` 并建议 `uth-debug`。
6. 必须执行仓库规定的 `clean assembleDebug --console=plain --no-daemon --stacktrace`，并扫描 warning/deprecated/exception/failed；要求全部为 0。
7. 不得执行 Git 写入，不得改 Design、docs/context 或加入 Design 外功能。
8. 写 `docs/work/D26071001-UI重构-Bot页/61-D26071001-T06-feedback-全链路集成与回归验收.md`，列出完整命令、追踪矩阵、0/0 证据、真实未验证设备项和 `Needs uth-docs scoped-sync`。
9. governed Markdown pre/post UTF-8 guard；仅在全部自动化验证通过时把 `docs/current-state.md` 更新为“T01–T06 已实现，待 uth-review”，不得宣称 Design-level 审查通过。

## 特别复查项

- T01：真实设备 migration instrumentation 若环境不可用，明确列为未验证，不得写 pass。
- T02/T03：真实 SAF/云来源与恢复设备行为若不可用，列出；静态/单测证据须明确区分。
- T04：真实键盘、外部点击、浅深主题、小屏与裁切视觉。
- T05：真实双指手感、面板拖动手感与最终视觉截图。
- T05 Rework 1 后裁切缩放不得退回滑杆；纵向/方形都需 pinch。

## 回传格式

最终回复不超过 18 行，包含：

- `Status: DONE | DONE_WITH_CONCERNS | BLOCKED | NEEDS_CONTEXT`
- 集成修复概览（没有也明确写无）
- requirement → evidence 覆盖结论
- 五条 T06 验证命令逐项结果
- `clean assembleDebug` 结果与 warning/deprecated/exception/failed 计数
- Feedback 路径、current-state 状态、UTF-8 guard
- 所有真实未验证设备项
- `Needs uth-docs scoped-sync`
- 未执行 Git 写入

没有新鲜完整构建证据时不得报 DONE。

## Rework 1 - 2026-07-11 15:59

Reason:

- 最终 reviewer 发现 T04/T05 三项阻断问题；原 workers 已分别完成 T04 Rework 1 与 T05 Rework 2。
- reviewer 修复后产生新代码，原 T06 fresh `clean assembleDebug` 证据已不再覆盖当前工作区。

Additional instructions:

1. 重新读取 T04/T05 最新 Feedback，核实目录/详情 crop center+zoom、模式二方卡、commit 失败 retry/discard 修复已落地。
2. 重新执行 T06 五条完整验证命令，包括 fresh `clean assembleDebug`；不得复用旧日志作当前证据。
3. 对新增 reviewer regression tests 使用 `--rerun-tasks` 或等价 fresh 方式确认实际执行。
4. 更新同一 T06 Feedback，追加 reviewer rework 后的验证批次与日志计数；更新 current-state 为“修复后待同一 reviewer 复审”。
5. 未验证设备项继续如实列出。不得 Git 写入。

Validation:

- 五条命令全部 exit 0。
- warning/deprecated/exception/failed = 0/0/0/0。
- 新增 crop render、方卡、failed draft retry/discard 测试均有 fresh 证据。

Return requirements:

- 明确 fresh 构建时间/耗时、日志计数和 reviewer 三项 finding 的回归证据。
