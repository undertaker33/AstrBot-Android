# P260711-1347 T04 Worker Prompt：人格目录双模式与顶栏交互

## 任务定位

- Scene：`uth-dev`
- Mode：`todo-implementation`
- 当前 Todo：`T04 人格目录双模式与顶栏交互`
- 前置：T01、T02 已提供 tags 与 cover/crop 能力；T03 已完成备份，不应在本 Todo 改动。
- 工作目录：`C:\Users\93445\Desktop\Astrbot\ElymBot-Android-Native`
- 你是本 Todo 唯一实现 worker；用户明确不设逐 Todo reviewer。

## 必读文件与视觉基准

完整读取 `AGENTS.md`、`docs/README.md`、`docs/current-state.md`、Design、T04 Todo、T01/T02 Feedback、`docs/context/02-应用壳层与集成.md`、`docs/context/06-Provider配置Bot与Persona.md`、相关 presentation/navigation 代码和测试。

视觉基准位于：

- `docs/work/D26071001-UI重构-Bot页/Image/定稿/模式一定稿.png`
- `docs/work/D26071001-UI重构-Bot页/Image/定稿/模式二定稿.png`
- `docs/work/D26071001-UI重构-Bot页/Image/定稿/搜索框.png`
- `docs/work/D26071001-UI重构-Bot页/Image/定稿/筛选弹窗.png`
- `docs/work/D26071001-UI重构-Bot页/Image/定稿/三点.png`

必须实际查看图片；底栏以现有 Android 代码为准，不照搬定稿图。

## 执行要求

1. 只实现 T04；不做详情表单或两步裁切 UI，不改 Room/备份/文件生命周期。
2. reducer、模式持久化、overlay、搜索筛选行为先 TDD；纯 Compose 视觉可按 Todo 的测试与 source contract 验证。
3. 保持“人格”命名、原机器人/模型/人格页面顺序、`MainSwipeRailState` 外层滑动与全局底栏不变；不得创建 Role 页面或聚合 Bot/Config/QQ 状态。
4. 模式一为沉浸单卡、卡内正式左右图标切换、纵向 crop；模式二为双列固定尺寸矩形、右列固定下沉 `64–72dp`、方形 crop。
5. 搜索/筛选/更多 overlay 互斥；搜索约 75% 宽并处理键盘/外部点击；筛选固定尺寸、内部滚动、pending/apply；更多只有新增人格与反向模式切换。
6. 正式图标使用 Material Icon / Vector Drawable，不得用文本字符或 Emoji。无新增 FAB、无内容区搜索/chip 行。
7. 模式偏好 production implementation 走 Hilt；不新增静态入口或第二套 DI。
8. 不得执行 Git 写入，不得覆盖前置或其他既有改动，不得超出 Todo 允许范围。
9. 运行 T04 全部验证命令；写 Feedback：`docs/work/D26071001-UI重构-Bot页/41-D26071001-T04-feedback-人格目录双模式与顶栏交互.md`。
10. governed Markdown 前后执行 UTF-8 guard；仅在 T04 完成并有新鲜证据时将 current-state 推进 T05。

## 自检与回传

逐项核对 T04 最终 `## Checklist`。特别检查主题切换不改变模式、首尾/单项箭头、固定卡片尺寸与错位、长名称/三标签截断、overlay 互斥，以及原导航/底栏 source contract。

最终回复不超过 15 行，包含 `Status`、修改概览、RED/GREEN、验证及 warning/exception/failed 计数、Feedback、未验证设备/视觉项、未执行 Git。若需超 scope 或视觉要求与现有架构冲突，立即 `NEEDS_CONTEXT`，不得擅自改外层导航。

## Rework 1 - 2026-07-11 15:34

Reason:

- 最终 reviewer 发现目录封面只把 `centerX/centerY` 映射为 alignment，完全忽略 `crop.zoom`，用户裁切缩放未进入模式一/模式二最终渲染。
- 模式二卡片固定 `260.dp` 高、图片区 `174.dp`，卡宽随屏幕变化，不满足 Design 的双列等宽等高方卡，也使 square crop 落入非方形 viewport。

Additional instructions:

1. 由原 T04 worker 修复 `PersonaCatalogUi`，建立可复用的 crop 渲染组件/策略，同时消费中心和 zoom；模式一使用 portrait，模式二使用 square。
2. 渲染必须以明确 viewport/contentScale 计算，非默认 zoom 应实际改变显示比例，而不是只改变 state 数值。
3. 模式二每张卡片必须通过 `aspectRatio(1f)` 或等价尺寸合同保持方形，双列等宽，右列继续固定下沉 64–72dp；图片/名称/标签变化不能改变卡片尺寸。
4. 先补失败的渲染策略/源码合同与不同宽度方卡合同测试，再实现 GREEN；测试必须覆盖非默认 zoom 和 portrait/square 选择。
5. 更新同一 T04 Feedback，追加 reviewer finding 与修复证据。current-state 在 T04 rework 完成前不得宣称全任务待 review。
6. 运行 T04 全部验证命令；不得 Git 写入，不得修改 T05 editor failure finding（由原 T05 worker负责）。

Validation:

- 给出非默认 zoom 最终渲染证据、模式二不同宽度方卡证据。
- presentation/app/modulePersonaCheck/architectureCheck 全通过，warning/exception/failed 为 0。

Return requirements:

- 明确说明 reviewer 的 Critical 目录部分与 Important 方卡 finding 是否均已修复。
