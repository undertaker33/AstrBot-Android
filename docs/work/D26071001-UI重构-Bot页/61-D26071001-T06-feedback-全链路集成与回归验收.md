# D26071001-T06 全链路集成与回归验收 Feedback

更新时间：2026-07-11 +08:00

## 状态与范围

- Scene：`uth-dev`
- Mode：`todo-implementation`
- 状态：T06 自动化验收通过；T01–T06 已实现，待 `uth-review` 做 Design-level 验收。
- 集成修复：无。规定验证与强制重跑均未暴露明确集成缺陷，因此未修改生产代码或测试。
- Git：未执行任何 Git 写入。

## Requirement → evidence 追踪

| Requirement | 自动化证据 | 结论 / 边界 |
| --- | --- | --- |
| Room 23→24、标签顺序、封面 metadata、级联删除 | `:core:db:testDebugUnitTest`、`:feature:persona:data:testDebugUnitTest`；两份 `24.json` SHA-256 均为 `39B2F4ADDBBFB05A0F84A75A30FE6AF7277D7ED70750857F321AA1A50BAA2B52` | JVM contract 与双 schema 一致通过；真实设备 migration instrumentation 未执行。 |
| 文件生命周期、来源只读、替换补偿、删除、orphan、路径逃逸 | persona data tests、`*Persona*` app tests、`PersonaCoverArchitectureContractTest` | 静态/单元证据通过；真实 SAF、云来源与导入后设备离线行为未执行。 |
| 完整/Persona 备份、老包、损坏图、冲突与 rollback | `*AppBackup*`、`moduleSettingsCheck`、`architectureCheck` | JVM/合同证据通过；真实设备 SAF 导出恢复未执行。 |
| 双模式、模式一箭头、模式二错位、顶栏互斥、搜索筛选 | persona presentation tests、`*Persona*`、`*MainSwipe*` | 状态与源码合同通过；定稿图已逐张查看，像素级/设备视觉未声称通过。 |
| 五字段、两态面板、动态居中、纵向/方形两步 crop 与 pinch | persona presentation tests、强制 `--rerun-tasks` | 状态、约束和双步骤/pinch 合同通过；真实触控手感、拖动吸附和键盘未执行。 |
| Bot/模型/外层滑动/全局底栏/引用保护/runtime context/其他备份 | `*MainSwipe*`、`*RuntimeContext*`、`*Persona*`、`*AppBackup*`、module/architecture checks | 自动化回归通过。 |
| 用户可见命名为“人格”，无新增 Role 页面/路由 | persona/navigation 源码扫描、architecture/app tests | 生产 Persona/navigation 目标路径无 `角色` / `Role` 命中；Design 图片历史文字不计为生产文案。 |

## T06 五条规定命令

1. `:core:db:testDebugUnitTest :feature:persona:data:testDebugUnitTest :feature:persona:presentation:testDebugUnitTest`：PASS，`BUILD SUCCESSFUL`；另以 `--rerun-tasks` 强制重跑 PASS。
2. `:app:testDebugUnitTest --tests "*Persona*" --tests "*AppBackup*" --tests "*MainSwipe*" --tests "*RuntimeContext*"`：PASS，`BUILD SUCCESSFUL`；另以 `--rerun-tasks` 强制重跑 PASS。
3. `modulePersonaCheck moduleSettingsCheck`：PASS，`BUILD SUCCESSFUL`。
4. `architectureCheck`：PASS，`BUILD SUCCESSFUL`。
5. `clean assembleDebug --console=plain --no-daemon --stacktrace`：PASS，fresh clean build，`BUILD SUCCESSFUL in 5m 12s`。

全部七份日志扫描：warning `0`、deprecated `0`、exception `0`、failed `0`。日志保存在本地 `.uth-sp/t06-logs/`，不作为受治理交付文档。

## 真实未验证设备项

- 当前环境无 `adb`，未执行真实设备 Room 23→24 migration instrumentation。

## Git baseline（v1.2.1 发布收口）

- 唯一 v1.2.0 基线：`dd9a04ac9c1b290178eed22c2cc262b7e0369dcc`（`codex/ColorOS16(RealmeUI7)`）。
- Persona feature commit：`cf0a78f`（`feat(persona): redesign persona experience`）。
- v1.2.1 release commit：`5c047a8`（`chore(release): prepare v1.2.1`）。
- Room v25 CI 合同修正：`e30e00c`（`test(db): align migration contract with Room v25`）。
- PR：[#28](https://github.com/undertaker33/ElymBot/pull/28)，已合并；merge commit：`a5f1ba52ad95451319f77491f92d21e78a7bcd79`。
- GitHub required CI：Build tooling、Feature module checks、App debug unit tests、Architecture contracts and debug assemble 两套运行均通过；DeepSource Kotlin 外部质量指标失败，不属于 required gate，其余 DeepSource 分析通过。
- 远端 `master` 已移除非用户提交 `27fd269fbad8b9bb7b0f8766bc7b50735e1afd9e` 及其 merge `72b9aea`；发布 lineage 不包含这两个提交。
- 正式 tag `v1.2.1` 将指向包含本节的 release closure commit；该 commit hash 因 Git 对象不能自引用，记录在 `uth-git` closeout 与 tag ref 中。
- 未执行真实 SAF/云图片来源、断网导入、用户原图保护和设备侧备份导出/恢复行为。
- 未执行真实键盘、外部点击、浅/深主题、小屏、长名称、三标签、超长提示词、单 Persona、空 Persona 的设备视觉检查。
- 未执行纵向/方形双指 pinch 手感、编辑面板拖动/吸附手感、动态居中最终视觉截图。

## 文档与后续

- UTF-8 guard：写前通过；写后结果见本轮 closeout。
- 未宣称 Design-level 审查通过；下一场景为 `uth-review`。
- `Needs uth-docs scoped-sync`

## Rework 1：reviewer 修复后的全量复验

- 时间：2026-07-11 16:00 +08:00 后。
- T04 目录 crop：`PersonaCoverRenderPolicyTest` 与 `PersonaCatalogCropSourceContractTest` 覆盖 portrait/square 独立 center+zoom、最终缩放和 `aspectRatio(1f)` 方卡合同。
- T05 详情与 draft：`PersonaEditorStateTest` 覆盖动态 viewport 使用持久化 portrait center+zoom，以及 commit 失败保留 draft/crops、retry 和 discard。
- 专项 fresh 命令使用 `--rerun-tasks`，`BUILD SUCCESSFUL in 1m 11s`，55 个任务实际执行。

五条 T06 命令全部重新执行：三模块窄测试 `29s` PASS；App 筛选测试 `44s` PASS；双 module checks `2m 15s` PASS；`architectureCheck` `1m 7s` PASS；fresh `clean assembleDebug` `4m 29s` PASS。

本批次六份新日志独立扫描：warning `0`、deprecated `0`、exception `0`、failed `0`；未复用原 T06 日志。两份 schema 24 SHA-256 仍一致：`39B2F4ADDBBFB05A0F84A75A30FE6AF7277D7ED70750857F321AA1A50BAA2B52`。

- 集成修复：无；reviewer 三项 finding 的修复由 T04/T05 workers 完成，本轮只做复验。
- 当前状态：T01–T06 已实现，reviewer 修复后待同一 reviewer 复审；未宣称 Design-level 审查通过。
- 真实设备未验证项保持不变：Room migration instrumentation、SAF/云来源与设备恢复、键盘/外部点击/浅深主题/小屏视觉、双指 pinch 与面板拖动手感、动态 viewport 最终截图。
- `Needs uth-docs scoped-sync`
- 未执行 Git 写入。

## Rework 2：T02 真实 manager I/O 修复后的 fresh 全链路复验

- 时间：2026-07-11 +08:00；验收对象为 T02 Rework 1 的 metadata 首次失败后同一 draft 可真实 retry，或由 discard 清理，以及该修复后的 T06 全链路回归。
- 专项 fresh 命令：`.\gradlew.bat :feature:persona:data:testDebugUnitTest --tests "*PersonaCover*" --rerun-tasks --console=plain --no-daemon --stacktrace`，exit `0`，`BUILD SUCCESSFUL in 2m 13s`。真实 `PersonaCoverAssetManager` 文件 I/O 测试确认首次 commit 失败后 draft/pending 保留且旧状态不变，第二次 commit 可发布并清理 draft/旧封面；失败后 discard 仅清理 draft。
- T06 命令 1：`.\gradlew.bat :core:db:testDebugUnitTest :feature:persona:data:testDebugUnitTest :feature:persona:presentation:testDebugUnitTest --console=plain --no-daemon --stacktrace`，exit `0`，耗时 `1m 09s`。
- T06 命令 2：`.\gradlew.bat :app:testDebugUnitTest --tests "*Persona*" --tests "*AppBackup*" --tests "*MainSwipe*" --tests "*RuntimeContext*" --console=plain --no-daemon --stacktrace`，exit `0`，耗时 `1m 13s`。
- T06 命令 3：`.\gradlew.bat modulePersonaCheck moduleSettingsCheck --console=plain --no-daemon --stacktrace`，exit `0`，耗时 `3m 56s`。
- T06 命令 4：`.\gradlew.bat architectureCheck --console=plain --no-daemon --stacktrace`，exit `0`，耗时 `1m 14s`。
- T06 命令 5：`.\gradlew.bat clean assembleDebug --console=plain --no-daemon --stacktrace`，fresh clean build exit `0`，`BUILD SUCCESSFUL in 6m 40s`。
- 本批次六份独立新日志统一扫描：warning `0`、deprecated `0`、exception `0`、failed `0`；日志位于 `.uth-sp/t06-logs/t02-rework1-fresh/`，未复用旧 T06 日志。
- 双 schema 24 SHA-256 仍一致：`39B2F4ADDBBFB05A0F84A75A30FE6AF7277D7ED70750857F321AA1A50BAA2B52`。
- 集成修复：无；本轮只验证 T02 worker 已完成的真实 manager I/O 修复，未修改生产代码或测试。
- 当前状态：T01–T06 已实现，T02 Rework 1 后 fresh 全链路通过，待同一 reviewer 复审；未宣称 Design-level 审查通过。
- 真实设备未验证项保持不变：Room migration instrumentation、SAF/云来源与设备恢复、键盘/外部点击/浅深主题/小屏视觉、双指 pinch 与面板拖动手感、动态 viewport 最终截图。
- `Needs uth-docs scoped-sync`
- 未执行 Git 写入。
